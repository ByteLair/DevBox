# DevBox - Dev Container Configuration

This directory contains VS Code Dev Container configuration.

## 🚀 How to use:

### Option 1: Open in VS Code (Recommended)

1. Install VS Code extension: "Dev Containers" (ms-vscode-remote.remote-containers)
2. Open this folder in VS Code
3. VS Code will detect the dev container configuration
4. Click "Reopen in Container" or press `Ctrl+Shift+P` > "Dev Containers: Reopen in Container"

### Option 2: Remote SSH (Already configured)

1. Install VS Code extension: "Remote - SSH"
2. Press `Ctrl+Shift+P` > "Remote-SSH: Connect to Host"
3. Select "my-workspace" or "workspace-dev"

## 📝 Configuration Files:

- **devcontainer.json** - Main configuration for Dev Containers
- Configures VS Code settings, extensions, and environment

## 🔧 Customization:

Edit `devcontainer.json` to:
- Add VS Code extensions automatically
- Configure environment variables
- Set up port forwarding
- Run post-creation scripts

## 📚 More Info:

https://code.visualstudio.com/docs/devcontainers/containers
